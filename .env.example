# Copy to `.env` (in repo root or scripts/) and fill in.
# Never commit the real .env.

ANTHROPIC_API_KEY=sk-ant-...

# Anthropic API key used for the public /advice/quick endpoint (anonymous users).
# Distinct from per-user encrypted keys in user_settings; sized for public traffic.
ANTHROPIC_API_KEY_ANONYMOUS=sk-ant-...
